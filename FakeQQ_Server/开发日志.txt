存在的问题：

###operate方法还没写好（程序在开始服务之后就必须维护一个在线用户表

###(如何进行多线程调试
###没有捕获绑定端口的异常
###ServerOperation类还没能调用Form2里面的控件
###启动服务的按钮不能按下多次，会出现一个端口只能绑定一次的异常

---------------------------------------------------------------
###许多种类型的数据包没有确定怎么使用
DataPacket类的content永远是string类型，程序在发送数据包前/接收数据包后，通过对象的序列化与反序列化来从数据包的content成员中获取不同的数据对象。

###启动服务的按钮的功能都写在form2里面了，不应这样而应该用类封装，form2只应调用，不应实现功能
思路不对，要是另设一个类，把button事件的功能全实现了，就算这个类能够修改窗口中的控件，但是这个类也无法在其他地方被重用，那就和最初代码重用的目标背道而驰了
